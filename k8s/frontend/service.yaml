apiVersion: v1
kind: Service
metadata:
  name: vedic-frontend-service
spec:
  selector:
    app: vedic-frontend
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP   # Ingress handles external traffic; no public LB needed